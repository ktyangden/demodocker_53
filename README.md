## Practical 53

Multi container Profile Web App on Docker

## Run App

To run the project locally:

```bash
npm install
cd demodocker
docker-compose up --build
```

Then visit:

```
http://localhost:8081/
```
```
http://localhost:3000/
```
